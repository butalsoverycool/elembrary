import { withTheme } from '../constants/Theme';

import button from './Button';
import { MainTitle, Title, Subtitle, Strong, Text } from './Text';
import { Row, Column } from './Flex';

export const Button = withTheme(button);
export { MainTitle, Title, Subtitle, Strong, Text, Row, Column };
